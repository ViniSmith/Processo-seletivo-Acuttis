import React from 'react';
import './Header.css';

export default () => {
    return (
        <header>
            <nav>
                <img src="./acuttis.png" alt=""/>
            </nav>
        </header>
    );
}